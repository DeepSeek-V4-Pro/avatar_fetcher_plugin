# ============================================================
# 头像获取插件 — 配置文件
# ============================================================

[plugin]
# 是否启用插件（true=启用, false=禁用）
enabled = true
# 配置版本号（请勿手动修改）
config_version = "1.0.0"
